---
title: تایپوگرافی
section:
    slug: core
    title: مفاهیم پایه
---

<Preview>
    {Object.keys(defaultConfig.theme.fontSize).map((size, index) => {
        return (<p key={index} className={`text-${size}`}>این بهترین است و چیزی بهتر از این نیست</p>)
    })}
</Preview>

import defaultConfig from 'tailwindcss/defaultConfig'
